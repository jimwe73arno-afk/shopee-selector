[build]
  # ✅ 不需要 build - 靜態 HTML 專案
  command = ""
  # ✅ 根目錄就是網站
  publish = "."

[functions]
  directory = "netlify/functions"

# ✅ 60 秒 timeout 給 3.0 Pro 足夠時間
[functions."*"]
  timeout = 60

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
