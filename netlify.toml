[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"

# ✅ 關鍵：增加 function timeout
[functions."*"]
  timeout = 60

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
