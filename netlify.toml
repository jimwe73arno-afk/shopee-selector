# Netlify configuration for BrotherG Shopee Selector

[build]
  command = ""
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"

# Correct timeout syntax (optional)
[functions.timeout]
  duration = "26s"
