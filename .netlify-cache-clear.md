# ⚠️ 重要：清除 Netlify 緩存指南

## 問題診斷

如果你在 Netlify 日誌中看到：
```
Error fetching from https://generativelanguage.googleapis.com/v1beta/models/...
status: 404
```

這表示 Netlify 仍在使用的**緩存中的舊版本 SDK**，它使用的是 v1beta API。

## 解決方法

### 方法 1：通過 Netlify Dashboard（推薦）

1. 登入 Netlify Dashboard
2. 選擇你的網站（shopee-selector-deploy）
3. 進入 **Deploys** 頁面
4. 點擊右上角 **"Trigger deploy"** 按鈕
5. **勾選 "Clear cache and deploy site"** ⚠️（這一步非常重要！）
6. 點擊 **"Deploy site"**
7. 等待部署完成（約 2-3 分鐘）

### 方法 2：通過 Netlify CLI

```bash
# 安裝 Netlify CLI（如果還沒安裝）
npm install -g netlify-cli

# 登入
netlify login

# 清除緩存並部署
netlify build --context production --clear-cache
netlify deploy --prod
```

## 驗證部署成功

部署完成後，檢查日誌應該看到：

✅ **成功標誌：**
- `📡 Expected endpoint: https://generativelanguage.googleapis.com/v1/models/...`
- `✅ Image X OCR completed`
- `✅ Map Phase complete`

❌ **如果還看到 v1beta：**
- 再次清除緩存並重新部署
- 檢查 `package.json` 是否正確更新到 `@google/generative-ai@^0.24.1`

## 當前配置

- SDK: `@google/generative-ai@^0.24.1`
- 模型: `gemini-1.5-flash` (Map) / `gemini-1.5-pro` (Reduce)
- API 版本: `v1` (正式版)


